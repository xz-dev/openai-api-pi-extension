{
  "name": "openai-api-extension",
  "version": "0.2.0",
  "description": "Minimal modern Pi provider extension for any OpenAI-compatible gateway using the Responses API.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xz-dev/openai-api-extension.git"
  },
  "files": [
    "index.ts",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "keywords": [
    "pi-package",
    "pi-extension",
    "openai",
    "responses-api"
  ],
  "type": "module",
  "scripts": {
    "check": "node --check index.ts && node --test tests/*.test.ts",
    "test": "node --test tests/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2"
  }
}
